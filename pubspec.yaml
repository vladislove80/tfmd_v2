name: tfmd_v2
description: Ttest task for the developer position Flutter Mobile Developer in ProArea

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_bloc: ^7.2.0
  injectable: ^1.5.0
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  geolocator: ^7.7.0
  geocoding: ^2.0.1
  http: ^0.13.3
  shared_preferences: ^2.0.7
  dio: ^4.0.0
  connectivity: ^3.0.6
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.4
  injectable_generator: ^1.4.1
  hive_generator: ^1.1.1

  flutter_localizations:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/media/images/

